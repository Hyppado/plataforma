{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate && next build",
  "git": {
    "deploymentEnabled": {
      "develop": true,
      "main": false
    }
  }
}
