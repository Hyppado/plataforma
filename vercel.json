{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate && next build",
  "git": {
    "deploymentEnabled": {
      "develop": true,
      "main": true
    }
  },
  "crons": [
    {
      "path": "/api/cron/echotik",
      "schedule": "0 * * * *"
    }
  ]
}
