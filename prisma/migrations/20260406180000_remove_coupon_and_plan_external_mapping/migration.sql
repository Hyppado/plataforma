-- DropTable: PlanExternalMapping (unused — zero runtime reads/writes)
DROP TABLE IF EXISTS "PlanExternalMapping";

-- DropTable: Coupon (pure Hotmart catalog mirror — no internal usage)
DROP TABLE IF EXISTS "Coupon";
